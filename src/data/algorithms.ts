// Реестр алгоритмов Менеджера процессов In.Plan.
//
// ОТКУДА ДАННЫЕ. Списки сняты 07.09.2026 в аутентифицированной сессии владельца
// карт на экране «Менеджер процессов» (`/process-manager`, комбобокс «Процесс»,
// фильтр по «MEIO» и по «DP») и продублированы реестром `/process` («Процессы»,
// столбец `name`). Это ФАКТ О СТЕНДЕ, а не проектное решение: если в платформе
// заведут новый алгоритм, здесь его не появится само.
//
// ПОЧЕМУ ССЫЛКА ОДНА НА ВСЕХ. Диплинка на конкретный алгоритм платформа не
// даёт — проверено: выбор процесса в комбобоксе не меняет ни путь, ни строку
// запроса, URL остаётся `/process-manager`. Поэтому имя алгоритма живёт в
// `title` ссылки, а `url` у всех алгоритмов один и тот же — экран целиком.
// Как только у платформы появится адрес вида `/process-manager?process=<id>`,
// менять придётся только `PROCESS_MANAGER_URL` и таблицу ниже.
//
// ЗАЧЕМ ЭТО В КОДЕ, А НЕ В process.json. Реестр — это ПОДСКАЗКА редактору
// (датасписок в форме ссылки), а не содержание карты. Содержание приходит
// конвейером из authoring source; список же нужен, чтобы владелец выбирал имя
// из реального перечня, а не печатал его по памяти с опечаткой.

/** Экран «Менеджер процессов»: единственный адрес, который даёт платформа. */
export const PROCESS_MANAGER_URL =
  'https://front-prod.k8s.b1gts1igqpnk9ib1350k.in-plan.ru/process-manager';

/** Реестр «Процессы» — таблица со всеми алгоритмами (столбцы name, description). */
export const PROCESS_REGISTRY_URL =
  'https://front-prod.k8s.b1gts1igqpnk9ib1350k.in-plan.ru/process';

/**
 * Алгоритмы модуля MEIO — 18 записей, порядок реестра (алфавитный по платформе).
 *
 * Список ПРОКРУЧЕН ДО КОНЦА при съёме: выпадающий список платформы
 * виртуализован и отдаёт в DOM только видимое окно, поэтому «сколько видно» и
 * «сколько есть» — разные величины. Первый снимок дал 17 из 18 именно поэтому.
 */
export const MEIO_ALGORITHMS: readonly string[] = [
  'MEIO ABC - Классификация',
  'MEIO Выбор политики пополнения',
  'MEIO Копирование версий',
  'MEIO Мультиэшелональный оптимизатор',
  'MEIO Определение стратегии MTS-MTO',
  'MEIO Оптимизатор (изолированный)',
  'MEIO Передача нормативов по запасам в SNP',
  'MEIO Проверка данных на консистентность',
  'MEIO Расчет CV',
  'MEIO Расчет CV длительности поставок',
  'MEIO Расчет CV длительности производства',
  'MEIO Сегментация',
  'MEIO Усреднитель',
  'MEIO отчет по ошибке прогноза',
  'MEIO отчет по сегментации',
  'MEIO отчет по страховым запасам',
  'MEIO отчет сравнение со страховыми запасам',
  'MEIO проверка цепочки на целостность',
] as const;

/**
 * Алгоритмы модуля DP — 9 записей, список прокручен до конца.
 *
 * Фильтр комбобокса — подстрока имени, а не поле модуля: в реестре есть и
 * процессы без префикса модуля (например DEMAND_PLANNING), они в этот список
 * не попадают. Это не пропуск: карта привязывается к алгоритмам модуля.
 *
 * «SP перенос прогноза из DP» назван по модулю-ПОТРЕБИТЕЛЮ (SNP), но выдаёт его
 * фильтр «DP» и относится он к публикации прогноза, поэтому список DP его
 * включает.
 */
export const DP_ALGORITHMS: readonly string[] = [
  'DP FA',
  'DP FA (неочищ данные)',
  'DP Проверка данных на консистентность',
  'DP Публикация прогноза на лагах',
  'DP_light',
  'DP_main_multiple_pipes',
  'DP_multiple_pipes_light',
  'DP_segment_forecasting',
  'SP перенос прогноза из DP',
] as const;

/**
 * Подсказки для карты по её id.
 *
 * Карта, которой в таблице нет (snp, mrp), получает ПУСТОЙ список, а не список
 * чужого модуля: подсказать планировщику SNP имена алгоритмов MEIO значило бы
 * предложить заведомо неверную привязку. Пустой список не запрещает ввести имя
 * руками — датасписок только подсказывает.
 */
const BY_MAP: Record<string, readonly string[]> = {
  meio: MEIO_ALGORITHMS,
  dp: DP_ALGORITHMS,
};

export function algorithmsForMap(mapId: string): readonly string[] {
  return BY_MAP[mapId] ?? [];
}
